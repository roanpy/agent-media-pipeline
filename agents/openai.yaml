interface:
  display_name: "Agent Media Pipeline"
  short_description: "Build Plex-ready media with subtitles and safe repair"
  default_prompt: "Use $agent-media-pipeline to inspect or process authorized media with the configured quality, subtitle, artwork, and delivery policy."
